#functions help in reusability and modularity in Python

def average(a,b,c):
    avg = (a + b + c) /3
    # print(avg)
    return avg # the return statement is used inside a function to send a value back to the caller. When a function reaches a return statement, it stops execution and returns the specified value.

a = average(3,1,4)


